<!DOCTYPE html>
<html>
    <head>
        <title>Weather Website></title>
        <link rel='stylesheet' href='/stylesheets/style.css' />
        <!-- Required meta tags -->
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

        <!-- Bootstrap CSS -->
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
              integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
    </head>
    <script src="javascripts/login.js"></script>
    <script >
        document.addEventListener('DOMContentLoaded', function()
        {
            checkUser();
        }, false);
    </script>
    <body style="background-color: lightskyblue">
        <h1>Welcome!</h1>
        <h2><%= message %></h2>

        <div class="container-fluid" style="overflow: hidden">
          <div class="row" style="align-content: center; margin-left: 25%; width: 50%; display: none; text-align: center" id="errorsBox">
            <div class="alert alert-danger" role="alert">
              <h2>Correct the following problems:</h2>
              <ul id="errorsList"></ul>
            </div>
          </div>
        </div>


        <div class="container row">
            <div class="jumbotron col-sm-4 pull-center" style="background-color: lightsteelblue">
                <form action="/checkUser" method="post">
                    <div class="form-group">
                        <label>Email:</label>
                        <input class="form-control" type="text" id="loginEmail" name="email"></input>
                        <label>Password:</label>
                        <input class="form-control" type="password" id="loginPassword" name="loginPass"></input>
                        <button type="submit" class="btn btn-primary" id="submitLogin" style="margin-top: 15px">Submit</button>
                    </div>
                </form>
            </div>
        </div>

        <div class="container row">
            <div class="jumbotron col-sm-4 pull-center" style="background-color: powderblue">
                <form action="/register">
                    <h3>Are you new here?</h3>
                    <h4>Sign up!</h4>
                    <button type="submit" class="btn btn-primary">Register</button>
                </form>
            </div>
        </div>
    </body>
</html>